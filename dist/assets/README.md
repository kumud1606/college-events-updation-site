Place your real college media files in this folder:

- `college-hero.jpg` for the login background image
- `college-loop.mp4` if you later want to replace the image with a looping campus video

Right now the UI uses a styled fallback background if the image is not present.
